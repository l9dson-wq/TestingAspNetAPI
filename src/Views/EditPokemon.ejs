<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Testing API</title>
  <link rel="stylesheet" href="css/output.css">
</head>
<body>
  <h4 class="text-xl font-bold mx-5">Edit a pokemon</h4>

  <% if(pokemonInfo != undefined) { %>
    <div class="m-5 border p-5 rounded-xl w-fit mx-auto">
      <h4 class="font-bold">Create a new pokemon</h4>
      <form method="post" action="/updateRequest">
        <div class="flex flex-col">
          <label for="Name">Name:</label>
          <input type="text" name="Name" class="border-2" value="<%= pokemonInfo.name %>">
        </div>
        <div class="flex flex-col">
          <label for="PrimaryType">Primary type:</label>
          <input type="text" name="PrimaryType" class="border-2" value="<%= pokemonInfo.primaryType %>">
        </div>
        <div class="flex flex-col">
          <label for="SecondaryType">Secondary type:</label>
          <input type="text" name="SecondaryType" class="border-2" value="<%= pokemonInfo.secondaryType %>">
        </div>
        <div class="flex flex-col">
          <label for="TrainerId">Trainer Id:</label>
          <input type="number" name="TrainerId" class="border-2" value="<%= pokemonInfo.trainerId %>">
        </div>
        <input type="hidden" name="pokemonId" value="<%= pokemonInfo.id %>">
        <button type="submit" class="mt-4 mx-auto bg-blue-600 text-white py-1 px-4 rounded-full text-sm">Save</button>
      </form>
    </div>
  <% } %>

</body>
</html>